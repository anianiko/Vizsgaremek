package Tests;

import TestEnvironment.TestEnvironment;
import org.openqa.selenium.WebDriver;

public class TestDataInputFromSource extends TestEnvironment {

    public TestDataInputFromSource(WebDriver driver) {
        super(driver);
    }
}
